/**
 * Extend the base Actor entity by defining a custom roll data structure which is ideal for the YeahNoFerSure system.
 * @extends {Actor}
 */
export class YeahNoFerSureActor extends Actor {
	/** @override */
	// getRollData() {
	// 	const data = super.getRollData();
	// 	return data;
	// }
	// /** @override */
	// prepareData() {
	// 	super.prepareData();
	// 	const actorData = this.data;
	// }
}
